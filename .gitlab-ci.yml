stages:
  - test
  - lint

test app:
  image: python:latest
  stage: test
  script:
    - cd python
    - python tests.py

eslint :
  image: node
  stage : lint
  script:
    - cd bettertimetable-app/src/
    - npm install eslint
    - eslint Timetable.js
    - eslint index.js