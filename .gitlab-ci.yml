stages:
  # - test
  - lint

# test app:
#   image: python:latest
#   stage: test
#   script:
#     - cd python
#     - python tests.py

eslint :
  image: node:latest
  stage : lint
  script:
    - npm install -g eslint eslint-plugin-react # eslint-config-airbnb eslint-plugin-import eslint-plugin-jsx-a11y eslint-plugin-react-hooks
    - cd bettertimetable-app/src
    - eslint Timetable.js
    - eslint index.js